# TODO

- Keep `TODO.md` merged into `AGENTS.md` when the instructions should become standing repo guidance.
- Source loading cleanup:
  - Move remote loading orchestration to an application-level service.
  - Keep `TipSourceService` focused on source selection.
